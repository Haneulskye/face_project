# Minimal runtime dependencies for the deployed web/backend service
# (backend/api.py). The full requirements.txt also includes offline
# preprocessing/training tools (torch, pandas, kaggle, ...) that the
# live server never imports — keeping this list separate keeps the
# deployed image small and the build fast on free-tier hosting.

fastapi
uvicorn[standard]
python-multipart
opencv-python-headless
numpy
onnxruntime
mediapipe==0.10.14
Pillow
